default:
  extensions:
    Behat\MinkExtension:
      browser_name: chrome
      base_url: http://localhost
      sessions:
        default:
          chrome:
            api_url: "http://127.0.0.1:9222"
  suites:
    default:
      contexts:
        - ApiFeatureContext
        - WebFeatureContext